<!-- Form to add to comments -->
<%= form_with(model: [ @post, @post.comments.build ], local: true) do |form| %>
  <p>
  <!-- Enter comment -->
    <%= form.label :comment %><br>
    <%= form.text_area :comment %>
  </p>

  <!-- Enter submit -->
  <p>
    <%= form.submit %>
  </p>
<% end %>